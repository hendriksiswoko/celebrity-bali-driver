import type { InstagramPost } from "@/types/content";

/**
 * Manually curated social-proof images from Instagram.
 * The site does not depend on a live Instagram API — add or remove
 * entries here (and matching files in /public/images/instagram/) whenever
 * the client wants to refresh the selection.
 */
export const instagramPosts: InstagramPost[] = [];
