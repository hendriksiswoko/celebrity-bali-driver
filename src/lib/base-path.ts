// Single source of truth for the GitHub Pages project-page base path.
// Read by next.config.ts (build-time routing) and by withBasePath()
// (runtime <img> src, since unoptimized next/image doesn't auto-prefix it).
export const BASE_PATH = "/celebrity-bali-driver";
